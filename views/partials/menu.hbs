<nav>
  <ul>
    {{#each menu}}
      <li>
        <a href="{{this.path}}" {{#if (eq ../activePage this.title)}}class="active"{{/if}}>
          {{this.title}}
        </a>
      </li>
    {{/each}}
  </ul>
</nav>